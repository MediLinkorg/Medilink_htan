# --- HTAN segmentation service ---
# Torch: install the CUDA build matching your EC2 driver in the Dockerfile.
# Pinned to the stack the HTAN models were trained/exported with.
torch==2.3.1
torchvision==0.18.1
numpy==1.26.4
scipy==1.13.1
pillow==10.4.0
einops==0.8.0

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.8.2

huggingface-hub==0.24.5

# dev / test
pytest==8.2.2
httpx==0.27.0
